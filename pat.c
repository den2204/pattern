#include <stdio.h>         
#include <stdlib.h>
#include <limits.h>
#define forn(i, n) for (int i = 0; i < (int)(n); ++i)
typedef long long ll;


#ifndef _WIN32
#define lld "lld"
#else
#define lld "I64d"
#endif

int main(void)
{                                                      

	return 0;
}